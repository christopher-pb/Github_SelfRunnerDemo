name: Test Self Hosted Runner

on: [push]

jobs:
  test-runner:
    runs-on: self-hosted

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Test runner
        run: echo "Runner is working!"
